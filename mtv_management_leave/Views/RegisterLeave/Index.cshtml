
@{
    ViewBag.Title = "Register leave";
}

@*@Html.vCardPanel(
    (string)ViewBag.Title,
    "You can register leave at here",
     Html.vBootGridFor(new BootGridTableOption
     {
         AjaxParameters = null,
         ClassName = "table table-striped",
         GuiId = "register-leave-table",
         AjaxUrl = "/api/value/value",
         AjaxSettings = new AjaxSetting
         {
             Cache = false,
             RequestType = "GET"
         }
     },
        new List<BootGridColumnOption>
        {
            new BootGridColumnOption{ Sortable = false, ColumnHeaderName = "Full name", FormatJqueryHandler = "fullname()", Identify = true, MappingFrom = "FullName"},
            new BootGridColumnOption{ Sortable = true, ColumnHeaderName = "Lave time", MappingFrom = "LeaveTime"}
        }),
     new Dictionary<string, object>().vAdd("Register", "").vAdd
)*@

@(Html.vCardPanel()
        .SetHeader((string)ViewBag.Title)
        .SetSummary("You can register leave at here")
        .SetName("leave-panel")
        .AddBody(
            Html.vBootGridFor()
                .SetName("register-leave-table")
                .SetAjaxUrl("/api/value/value")
                .SetRequestType("GET")
                .AddColumn(new BootGridColumnOption { Sortable = false, ColumnHeaderName = "Full name", FormatJqueryHandler = "fullname()", Identify = true, MappingFrom = "FullName" })
                .AddColumn(new BootGridColumnOption { Sortable = false, ColumnHeaderName = "Full name", FormatJqueryHandler = "fullname()", Identify = true, MappingFrom = "FullName" })
        )
        .AddAction("Refesh", new {href="google.com", onClick="OnHandlerClick()"})
        .ToHtml()
)



